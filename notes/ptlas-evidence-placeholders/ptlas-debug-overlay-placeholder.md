# PTLAS Debug Overlay Placeholder

Status: capture needed

Purpose:

- capture a frame where touched partitions, moving instances, and global partition population are visible
- support the article's claim about backend command scope with visual diagnostics

Required overlay fields:

| Field | Value |
|---|---|
| scene instances | capture needed |
| changed-instance count | capture needed |
| rewritten-instance count | capture needed |
| touched-partition count | capture needed |
| global partition population | capture needed |
| native operation type/count | capture needed |
| TLAS update time | capture needed |
| PTLAS update time | capture needed |
| memory | capture needed |
| scratch | capture needed |

Screenshot requirements:

- show partition boundaries or touched partition overlay
- show global partition population if available
- show backend path or fallback state if available
- store source screenshot path here when captured: `capture needed`
