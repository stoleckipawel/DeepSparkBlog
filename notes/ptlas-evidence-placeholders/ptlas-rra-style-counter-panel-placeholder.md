# PTLAS RRA-Style Counter Panel Placeholder

Status: capture needed

Purpose:

- provide a tool-like counter panel inspired by AMD GPUOpen / RRA analysis style
- show counters before any performance conclusion

Panel fields:

| Counter | Classic TLAS | PTLAS | Source |
|---|---:|---:|---|
| scene instances | capture needed | capture needed | capture needed |
| changed-instance count | capture needed | capture needed | capture needed |
| rewritten-instance count | capture needed | capture needed | capture needed |
| touched-partition count | not applicable | capture needed | capture needed |
| global partition population | not applicable | capture needed | capture needed |
| native operation type/count | capture needed | capture needed | capture needed |
| top-level update time | capture needed | capture needed | capture needed |
| memory | capture needed | capture needed | capture needed |
| scratch | capture needed | capture needed | capture needed |

Visual design notes:

- dark tool-panel style
- compact metric cells
- no vendor logos
- no heatmap unless captured from the engine or a generated diagnostic overlay
